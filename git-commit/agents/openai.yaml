interface:
  display_name: "Git 提交信息规范"
  short_description: "按 Conventional Commits 规范生成提交信息"
  default_prompt: "使用 $git-commit 根据当前改动生成符合 Conventional Commits 规范的提交信息。"

policy:
  allow_implicit_invocation: true
